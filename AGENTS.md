# Agent instructions

- Do not build the project (e.g. `./gradlew assemble`, `./gradlew build`, or similar Gradle build/compile tasks). The user builds and runs it themselves, typically from Android Studio.
